import fs from 'fs'; import Papa from 'papaparse'; const csv = fs.readFileSync('downloadbyjanos/REPORTING PARA BASE DE DATOS - Sheet1 (4).csv', 'utf8'); const results = Papa.parse(csv, { header: true }); const seen = new Set(); const duplicates = []; results.data.forEach((row, index) => { const date = row['DATE']; const bu = row['BUISNESS UNIT']; if (date && bu) { const key = date + '|' + bu; if (seen.has(key)) { duplicates.push({ index, key }); } seen.add(key); } }); console.log('Duplicates found:', duplicates.length); if (duplicates.length > 0) console.log(duplicates.slice(0, 5));
